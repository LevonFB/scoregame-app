-- 0126: уточнение описания задания «Чтение игры» (dq_read_game).
-- Копирайт-правка, бизнес-логика не меняется: задание и раньше не засчитывало
-- попадание в исход только за счёт подстраховки double_chance — теперь это
-- явно сказано в описании. tasks_catalog имеет приоритет над копией в коде
-- (resolveQuestCopy), поэтому правка нужна и в данных.
-- Данные: обновляется одна строка tasks_catalog; прогресс/награды не затрагиваются.

UPDATE tasks_catalog
SET description = 'Угадайте исход матча своим прогнозом (двойной шанс не засчитывается)'
WHERE task_key = 'dq_read_game';
