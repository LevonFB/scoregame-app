-- 0145_app_task_season_ballon_dor.sql
-- Догоняет 0144: группа заданий «Золотой мяч» внутри «Задания» → «Прогнозы
-- сезона». Без этой строки ключ не попадал в реестр, и группу — единственную
-- из четырёх — нельзя было скрыть из админки (непрописанный ключ на фронте
-- означает «показывать всегда»).
--
-- Сеется visible_to_all, как и соседние группы. Закрытость самой фичи держит
-- отдельный ключ `season_predictions.ballon_dor` (admin_only) — он и остаётся
-- единственным рубильником «released / не released». Если бы группу тоже
-- засеяли admin_only, открытие вкладки игрокам пришлось бы делать в двух
-- местах, и про второе легко забыть.
-- Чистое UI-гейтирование: заданий, прогресса и наград не касается.

INSERT OR IGNORE INTO app_section_visibility (section_key, parent_key, title, visibility, sort_order) VALUES
  ('tasks.season.ballon_dor', 'tasks.season', 'Золотой мяч', 'visible_to_all', 734);
